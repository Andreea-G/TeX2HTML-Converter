% For explanations on how this file is used please see html_commands.tex

% ------- Pdf specific commands: -------
\usepackage{xcolor}
%\usepackage{framed}
\definecolor{backGround}{HTML}{\White}
\pagecolor{backGround}


\renewcommand\indent{}

% Restores the \boldsymbol command to what it was in latex
\let\boldsymbol=\oldbs
\renewcommand\bs[1]{\boldsymbol{#1}}

\renewcommand\DrawHLine{}

% Since videos cannot be included, nothing is added here. TODO: Will need to get back to this.
\renewcommand\includevideo[1]{}

% Redefined the htmlToggle environment. Toggling is not possible in a pdf. Instead this makes the "toggled" sections appear in a box of a given different color.
\renewenvironmentx{htmlToggle}[4][1=null, 4=\ZerothGray]{%
\definecolor{shadecolor}{HTML}{#4}%
%\begin{snugshade}%
}{%
%\end{snugshade}%
}

% For now these are redefined to do nothing. TODO: Will need to get back to this.
\renewenvironment{htmlAlert}[1]{}{}
\renewenvironment{fulljustify}{}{}
\renewenvironment{htmlColumns}[1]{}{}
